# Implementation Plans

Generated by the improve skill on 2026-06-10 (advisor session, LUMEN commit `759eb55`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

These four plans were requested explicitly by the owner (not audit findings):
form simplification, catalog categories + NE4 rate-card seed, personnel/teams
with person-level assignment, and NE4-bridge hardening.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Service catalog categories + NE4 rate-card seed | P1 | M | — | DONE (commit `020ff8a` on `feat/service-catalog-categories`, in executor worktree — pending merge by owner; operator code corrected to `WESTC`) |
| 002  | Order form: derive client/operator/line from project | P1 | M | 001 (same file) | DONE (committed on `feat/order-form-project-derivation`; preflight restored by 005) |
| 005  | Fix LoginPage conditional hook (restores repo-wide preflight) | P1 | S | — | DONE (preflight restored; demo admin login smoke passed) |
| 003  | Personnel teams + person-level assignment | P2 | L | 002 committed; 005 recommended first (refreshed 2026-06-11, see plan's Preconditions) | DONE (commit `f92ca0a`; preflight green; demo smoke passed) |
| 004  | NE4 bridge: provenance, metadata, sync log | P2 | M | — (migration `025` confirmed free; preconditions refreshed 2026-06-11) | DONE (LUMEN `0682e53` on `feat/ne4-bridge-provenance` + ne4 `369cfe1` on `feat/lumen-sync-observability`; preflight green; demo visual check pending; deploy order: LUMEN migration → redeploy lumen-bridge → ne4 migration) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **005 unblocks the gates**: a pre-existing conditional-hook lint ERROR in
  `src/pages/auth/LoginPage.tsx:73` (came with the upstream sync, commit
  `a9e499a`) makes `npm run preflight` fail on EVERY branch. Plans 003/004
  done criteria depend on that gate — run 005 first.
- **003 needs 002 committed**: plan 002's work was left uncommitted on
  `feat/order-form-project-derivation`; it shares three files with 003
  (fixtures, locales, workOrderService). Commit before dispatching 003.
- **002 after 001**: both edit `src/pages/admin/WorkOrderFormPage.tsx` (001 only
  the service-item `<select>`, 002 the rest). Sequential execution avoids
  conflicts; they are functionally independent.
- **Migration numbering is a shared sequence**: plans reserve 022 (catalog),
  023 (projects), 024 (employees), 025 (work_orders source) in LUMEN, plus 022
  in ne4-work-manager. Numbers are PROVISIONAL — every plan's Step 1 re-checks
  against the EsneiderCode/Lumen develop branch (`git ls-tree`). If you
  renumber, update the `Depends on:` headers of the later plans and this note.
- **004 has a cross-repo deploy-order constraint**: LUMEN migration applied →
  `lumen-bridge` function redeployed → ne4 migration applied. Code can be
  written in any order; deployment cannot.
- **004 also fixes a verified routing defect** (found 2026-06-11 while
  reviewing 001): ne4's bridge config maps the WC operator to LUMEN code
  `WESTCONNECT`, but LUMEN's operator code is `WESTC` — WC-cita syncs fail
  silently until this lands. Verify LUMEN prod for a manually created
  `WESTCONNECT` operator row before applying (see plan 004, Step 3 item 4).
- Never apply migrations from this machine; the repo owner applies them.
  `database.types.ts` is never edited by hand (use `as never` casts).

## Findings considered and rejected

- **Build the LUMEN↔ne4 bridge**: rejected as already done — a complete
  pg_net-trigger → edge-function pipeline exists and runs
  (`ne4-work-manager/supabase/functions/lumen-bridge/`, migrations 015/020).
  Plan 004 hardens it instead of rebuilding.
- **Strict project→operator derivation (non-overridable)**: rejected — demo
  data shows one project serving two operators; plan 002 uses overridable
  `default_*` columns instead.
- **Merging employees and operational users into one table**: rejected — the
  two entities serve different domains (payroll vs. auth/field-ops); plan 003
  links them (`employees.profile_id`) and unifies the VIEW, not the storage.
- **Repurposing `assigned_detail_snapshot` for NE4 execution data**: rejected —
  its documented semantics ("what the admin assigned, set once") would be
  inverted; plan 004 adds `external_metadata` instead.
- **Fixing the bridge's hardcoded `work_type: 'alta'`**: deferred, not planned —
  needs a business mapping from ne4 `workflow_code` that doesn't exist yet.
